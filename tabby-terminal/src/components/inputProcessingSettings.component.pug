.form-line
    .header
        .title(translate) Backspace key mode

    select.form-select([(ngModel)]='options.backspace')
        option(
            *ngFor='let mode of backspaceModes',
            [value]='mode.key',
        ) {{mode.name|translate}}
